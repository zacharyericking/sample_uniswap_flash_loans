Author: Zachary King - github.com/zacharyericking/sample_uniswap_flash_loans

# `test/utils/` Module

Objective: local testing scaffolding used across repository tests.

- `Test.sol`: lightweight `vm` interface and assertion helpers.

This directory keeps tests self-contained while preserving strong type checks in helper calls.
